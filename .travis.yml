sudo: required
services:
    - docker

script:
    - docker build -t mannydesigns/node .
    - docker images mannydesigns/node

before_deploy:
    - docker login -u vitaliifokine -p vf281992

deploy:
    provider: script
    script: docker push vitaliifokine/mannydesigns:node
    skip_cleanup: true
    on:
        branch: master
